docker {
       enabled = true
       temp = 'auto'
}

profiles {
         standard {
                  process.executor      = 'local'
         }

         cluster {
                 process {
                         executor = 'sge'
                 }
         }
}

executor {
         name          = 'sge'
         pollInterval  = '10sec'
         queueSize     = 100
}
